(function () {
  var btn = document.getElementById('btnOk');
  if (!btn) { alert('btnOk nenalezen'); return; }

  var p = btn;
  while (p && (!p.className || p.className.indexOf('form-group') === -1)) {
    p = p.parentNode;
  }
  if (p) {
    p.style.display = 'none';
  }
})();
